## hello, p5js

this directory contains p5js specific sketches
use the p5 utility to create new p5js sketches

Create a new sketch with default size (500, 500)
`$ p5 new sketch mySketch`

Create a new sketch with size (400, 400)
`$ p5 new sketch mySketch -s 400`

Create a new full screen sketch
`$ p5 new sketch mySketch -S`